/*

#include <>  ->  search in the default lib
#include ""  ->  search the lib in the dir of the folder

#define VAR(x) NUM  -> set the variable as NUM

Variables:  
%c char         1byte
%i integer      2bytes
&s short        2bytes
&l long         4bytes
%f float        4bytes
%d double       8bytes

ex:  %.3f  reduce the digits number

unsigned  -> variable without sign

int A ->  char A:  (char)A
!!! An int as a char looks like a letter in ASCII
EX:  (char)70 = P


scanf("%d",&num1);

printf("Resultado:%d\n",resultado);

if (---)
    {
        ----------------
    }
    else if (---)
    {
        ---------------
    }
    else{
        ----------------
    }

for (int i = 0; i < 5; i++)
    {
        printf("%i\n", i);
    }

while (contador <= 10)
    {
        printf("%i\n", contador);
        contador++;
    }

do
    {
        printf(contador);
        contador++;
    } while (contador <= 10));

switch (casos)
    {
    case 1:
        printf("wena 1\n");
        break;
    case 2:
        printf("wena 2\n");
    default:
        printf("no no no uwu\n");
        break;
    }

*/